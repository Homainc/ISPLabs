CREATE OR REPLACE FUNCTION DELETE_CATEGORY(
    P_ID IN NUMBER,
    ER OUT VARCHAR2)
    RETURN BOOLEAN
AS
BEGIN
    DELETE from FORUM_CATEGORY
    WHERE ID = P_ID;
    RETURN TRUE;
EXCEPTION
    WHEN OTHERS THEN
        ER := 'Delete failed for category';
    RETURN FALSE;
END;
    