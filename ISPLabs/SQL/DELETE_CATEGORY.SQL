CREATE OR REPLACE FUNCTION DELETE_CATEGORY(
    P_ID IN NUMBER,
    ER OUT VARCHAR2)
    RETURN NUMBER
AS
BEGIN
    DELETE from FORUM_CATEGORY
    WHERE ID = P_ID;
    RETURN 1;
EXCEPTION
    WHEN OTHERS THEN
        ER := 'Delete failed for category';
    RETURN 0;
END;
    
