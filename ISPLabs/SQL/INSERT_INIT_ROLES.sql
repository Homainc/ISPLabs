CREATE OR REPLACE PROCEDURE INSERT_INIT_ROLES 
IS
    ADMIN_NAME VARCHAR2(5) := 'admin';
    USER_NAME VARCHAR2(5) := 'user';
BEGIN
    INSERT INTO USER_ROLE
    (NAME)
    VALUES (USER_NAME);
    INSERT INTO USER_ROLE
    (NAME)
    VALUES (ADMIN_NAME);
END;