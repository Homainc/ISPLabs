CREATE OR REPLACE FUNCTION UPDATE_TOPIC(P_ID NUMBER, P_NAME VARCHAR2, P_IS_CLOSED NUMBER)
RETURN NUMBER
IS
BEGIN
 UPDATE TOPIC
 SET NAME = P_NAME, IS_CLOSED = P_IS_CLOSED
 WHERE ID = P_ID;
 RETURN 1;
EXCEPTION
WHEN OTHERS THEN
    RETURN 0;
END;
