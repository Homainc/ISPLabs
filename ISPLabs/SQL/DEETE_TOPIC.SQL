CREATE OR REPLACE FUNCTION DELETE_TOPIC(
    P_ID IN NUMBER,
    ER OUT VARCHAR2)
    RETURN NUMBER
AS
BEGIN
    DELETE from TOPIC
    WHERE ID = P_ID;
    RETURN 1;
EXCEPTION
    WHEN OTHERS THEN
        ER := 'Delete failed for topic';
    RETURN 0;
END;
