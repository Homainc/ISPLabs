CREATE OR REPLACE FUNCTION DELETE_TOPIC(
    P_ID IN NUMBER,
    ER OUT VARCHAR2)
    RETURN BOOLEAN
AS
BEGIN
    DELETE from TOPIC
    WHERE ID = P_ID;
    RETURN TRUE;
EXCEPTION
    WHEN OTHERS THEN
        ER := 'Delete failed for topic';
    RETURN FALSE;
END;